
## Pull Request Rules
- Never include `.github/workflows/*.yml` files in a Pull Request. These should be managed in a separate branch (e.g., `trigger`) or kept local to the fork and excluded from upstream-targeted commits.
